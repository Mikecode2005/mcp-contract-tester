# Examples

The JSON fixtures in this directory are synthetic and safe to commit. valid-tools.json demonstrates a passing tools document. invalid-tools.json and schema-errors.json demonstrate blocking findings. sample-trace.jsonl and trace-with-error.jsonl demonstrate local trace inspection.

Use these files as starting points for your own tests, but replace names and results with data that does not contain credentials or private customer information.
